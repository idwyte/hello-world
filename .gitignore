node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.local
.env.*.local
.DS_Store
# Native projects generated by `expo prebuild` are gitignored at the repo
# root (regenerated each build). Module-local ios/ and android/ trees under
# modules/* hold the hand-written Swift / Kotlin and must be tracked.
/ios/
/android/
.eas/
